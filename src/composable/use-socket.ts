import { io } from "socket.io-client";

// const socket = io("http://localhost:3000");

// socket.io.on('open', console.log)

export function useSocket() {
  function createRoom () {
    // socket.emit('room:create');
  }

  return {
    // socket,
  }
}
